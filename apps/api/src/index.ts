import Fastify from 'fastify'; import fastifyJwt from '@fastify/jwt'; import fastifyFormbody from '@fastify/formbody'; import { config } from 'dotenv'; import { fileURLToPath } from 'url'; import { dirname, join } from 'path'; import { readEnv } from './utils/env.js'; import { getDb } from './db/index.js'; import agentsPlugin from './routes/agents.js'; import roomsPlugin from './routes/rooms.js'; import settingsPlugin from './routes/settings.js'; import authPlugin from './routes/auth.js'; import usersPlugin from './routes/users.js'; import { createWsHandler, broadcast } from './ws/handler.js'; import { getRoomRunner } from './engine/room-runner.js'; // Load .env from project root const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename); config({ path: join(__dirname, '../../../.env') }); async function main(): Promise<void> { const env = readEnv(); // Initialize DB (creates tables if needed) const dbClient = await getDb(); // Initialize the room runner with the broadcast function getRoomRunner(broadcast); // Determine if auth is required (check if users exist) const userCount = await (dbClient.db as any).select().from(dbClient.schema.users); const hasUsers = userCount.length > 0; const app = Fastify({ logger: true }); // CORS — allow all origins (frontend on different port in dev) app.addHook('onRequest', async (_req, reply) => { void reply.header('Access-Control-Allow-Origin', '*'); void reply.header('Access-Control-Allow-Methods', 'GET,POST,PATCH,DELETE,OPTIONS'); void reply.header('Access-Control-Allow-Headers', 'Content-Type, Authorization'); }); // Body parser for JSON await app.register(fastifyFormbody); // Setup JWT - Uses Authorization: Bearer header by default await app.register(fastifyJwt, { secret: process.env.JWT_SECRET || 'super-secret-agentroom-key-998877', // No cookie config - reads from Authorization header by default }); // Require Authentication via JWT for API (if users exist) app.addHook('onRequest', async (req, reply) => { if (!hasUsers && !env.ADMIN_PASSWORD) return; // Open mode if no users/password set if (req.method === 'OPTIONS') return; // Pre-flight if (req.url === '/health' || req.url.startsWith('/api/auth')) return; try { await req.jwtVerify(); } catch { return reply.status(401).send({ error: 'Unauthorized' }); } }); app.addHook('preHandler', async (req, reply) => { if (req.method === 'OPTIONS') { return reply.status(204).send(); } }); // REST routes await app.register(authPlugin, { prefix: '/api' }); await app.register(agentsPlugin, { prefix: '/api' }); await app.register(roomsPlugin, { prefix: '/api' }); await app.register(settingsPlugin, { prefix: '/api' }); await app.register(usersPlugin, { prefix: '/api' }); // Health check app.get('/health', async () => ({ status: 'ok' })); // Start server const address = await app.listen({ port: env.PORT, host: '0.0.0.0' }); app.log.info(`AgentRoom API listening at ${address}`); // Attach WebSocket server to the underlying HTTP server createWsHandler(app.server); } main().catch((err: unknown) => { console.error('Fatal error:', err); process.exit(1); });